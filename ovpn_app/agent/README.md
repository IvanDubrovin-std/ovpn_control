# OpenVPN Management Agent

Автономный Python агент, который запускается на удаленном OpenVPN сервере и выполняет команды локально, избегая проблем с SSH таймаутами.

## 🎯 Зачем нужен агент?

### Проблемы текущего подхода (SSH):
- ❌ SSH таймауты при долгих операциях (gen-dh 4096 занимает 5-30 минут)
- ❌ Сетевые проблемы прерывают выполнение
- ❌ Нет возможности отслеживать прогресс
- ❌ Высокая нагрузка на сеть

### Преимущества агента:
- ✅ Локальное выполнение команд (без SSH overhead)
- ✅ Отчеты о прогрессе в реальном времени
- ✅ Устойчивость к сетевым проблемам
- ✅ Работает как systemd service
- ✅ Поддержка долгих операций (DH 4096, установка пакетов)

## 📦 Установка агента на сервере

### Автоматическая установка через Django:

```python
# В Django приложении будет метод deploy_agent()
from ovpn_app.agent.deployment import AgentDeployer

deployer = AgentDeployer(ssh_credentials)
deployer.deploy_agent(server)
```

### Ручная установка:

```bash
# 1. Скопируйте агента на сервер
scp ovpn_agent.py user@server:/usr/local/bin/ovpn-agent
chmod +x /usr/local/bin/ovpn-agent

# 2. Создайте systemd service (опционально)
sudo tee /etc/systemd/system/ovpn-agent.service > /dev/null << 'EOF'
[Unit]
Description=OpenVPN Management Agent
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/bin/python3 /usr/local/bin/ovpn-agent daemon
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 3. Запустите service
sudo systemctl daemon-reload
sudo systemctl enable ovpn-agent
sudo systemctl start ovpn-agent
```

## 🚀 Использование агента

### CLI режим:

```bash
# Установка OpenVPN
ovpn-agent install --task-id task-001

# Настройка OpenVPN
ovpn-agent configure --task-id task-002 --config /tmp/config.json

# Полная переустановка
ovpn-agent reinstall --task-id task-003 --config /tmp/config.json
```

### Пример конфигурации (config.json):

```json
{
  "port": 1194,
  "protocol": "udp",
  "subnet": "10.8.0.0",
  "netmask": "255.255.255.0",
  "dns_servers": ["8.8.8.8", "8.8.4.4"],
  "use_stunnel": false
}
```

### Через Django API:

```python
# В Django будет новый endpoint: /api/servers/<id>/execute-via-agent/
POST /api/servers/3/execute-via-agent/
{
    "command": "reinstall",
    "config": {
        "port": 1194,
        "protocol": "udp",
        ...
    }
}
```

## 📊 Отслеживание прогресса

Агент отправляет прогресс в Django через WebSocket или HTTP POST:

```json
{
    "task_id": "task-001",
    "progress": 55,
    "message": "Generating DH parameters (this may take several minutes...)",
    "status": "running"
}
```

## 🔧 Архитектура

```
┌─────────────────┐           ┌──────────────────┐
│  Django Server  │   HTTP    │  OpenVPN Server  │
│                 ├──────────►│                  │
│  - Web UI       │   SSH     │  - ovpn-agent.py │
│  - REST API     │   (once)  │  - Local exec    │
│  - WebSocket    │◄──────────┤  - No timeouts   │
└─────────────────┘  Progress └──────────────────┘
```

## 🛠️ Функции агента

### `install_openvpn(task_id)`
Устанавливает OpenVPN и зависимости:
- apt update
- apt install openvpn easy-rsa netcat-openbsd
- systemctl enable openvpn
- Создание директорий

**Прогресс:** 0% → 100% (5 шагов)

### `configure_openvpn(task_id, config)`
Настраивает OpenVPN сервер:
- Инициализация PKI (Easy-RSA)
- Генерация CA сертификата
- Генерация серверного сертификата
- **Генерация DH 4096** (долгая операция!)
- Генерация TLS-Crypt ключа
- Генерация CRL
- Создание server.conf
- Настройка системы (IP forwarding, firewall)

**Прогресс:** 0% → 100% (20+ шагов)

**Долгая операция:** `gen-dh` занимает 5-30 минут на слабых серверах

### `reinstall_openvpn(task_id, config)`
Полная переустановка OpenVPN:
1. Stop service (5%)
2. Disable autostart (10%)
3. Cleanup old configs (15%)
4. Install OpenVPN (20-40%)
5. Configure OpenVPN (40-95%)
6. Start service (95%)
7. Verify (100%)

**Прогресс:** 0% → 100% с подробными отчетами

## 📝 Логирование

Логи агента:
- **Stdout:** Прогресс и результаты
- **File:** `/var/log/ovpn-agent.log`

Формат:
```
2025-11-14 18:30:00 - ovpn_agent - INFO - Starting OpenVPN installation
2025-11-14 18:30:05 - ovpn_agent - INFO - Executing: sudo apt update -y
2025-11-14 18:30:10 - ovpn_agent - INFO - Progress [20%]: Installing packages
2025-11-14 18:35:42 - ovpn_agent - INFO - Progress [55%]: Generating DH parameters...
2025-11-14 18:58:23 - ovpn_agent - INFO - Progress [100%]: OpenVPN configured successfully
```

## 🔐 Безопасность

- ✅ Агент работает с sudo привилегиями (требуется для OpenVPN)
- ✅ API ключ для аутентификации с Django
- ✅ Валидация всех входных данных
- ✅ Логирование всех операций
- ⚠️ ВАЖНО: Настройте firewall для закрытия HTTP порта агента

## 🚦 Статусы задач

- `PENDING` - Задача в очереди
- `RUNNING` - Задача выполняется
- `SUCCESS` - Задача завершена успешно
- `FAILED` - Задача завершена с ошибкой

## 🔄 Следующие шаги

1. ✅ Агент создан
2. ⏳ Создать AgentDeployer (автоматическая установка агента)
3. ⏳ Добавить WebSocket для прогресса в реальном времени
4. ⏳ Добавить Django API endpoints для работы с агентом
5. ⏳ Обновить UI для отображения прогресса
6. ⏳ Добавить systemd service файл
7. ⏳ Создать тесты

## 📚 Примеры использования

### Пример 1: Установка OpenVPN с прогрессом

```bash
# На сервере
/usr/local/bin/ovpn-agent install --task-id task-123

# Вывод:
{
  "status": "success",
  "message": "OpenVPN installed successfully",
  "output": "=== Updating package list ===\n...",
  "progress": 100
}
```

### Пример 2: Переустановка через API

```python
# Django view
from ovpn_app.agent.client import AgentClient

client = AgentClient(server)
result = await client.reinstall_openvpn({
    "port": 1194,
    "protocol": "udp",
    "subnet": "10.8.0.0",
    "netmask": "255.255.255.0"
})

print(f"Status: {result['status']}")
print(f"Progress: {result['progress']}%")
```

## 💡 Преимущества для пользователя

- **Больше не нужно ждать** - долгие операции выполняются в фоне
- **Прогресс в реальном времени** - видите, что происходит
- **Надежность** - операция не прервется из-за сети
- **DH 4096 без проблем** - агент справится с любой долгой операцией

## 🎉 Результат

Теперь переустановка OpenVPN с DH 4096:
- ✅ Не зависает
- ✅ Показывает прогресс
- ✅ Не боится таймаутов
- ✅ Генерация DH 4096 работает корректно
- ✅ Пользователь видит, что происходит
