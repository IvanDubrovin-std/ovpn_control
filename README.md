# OpenVPN Web Management System

Django веб-приложение для управления OpenVPN серверами через веб-интерфейс.

## Возможности

- 🌐 Веб-интерфейс для управления серверами
- 🔧 Настройка OpenVPN серверов через веб-формы
- 🎫 Управление клиентскими сертификатами
- 📊 Мониторинг подключений в реальном времени
- 🔒 Безопасное SSH подключение к серверам
- � Автоматическая генерация и установка SSH ключей
- �👥 Система аутентификации пользователей
- 📡 WebSocket для real-time обновлений

## Установка и запуск

1. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Linux/macOS
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Выполните миграции:**
   ```bash
   python manage.py migrate
   ```

4. **Создайте суперпользователя:**
   ```bash
   python manage.py createsuperuser
   ```

5. **Запустите сервер разработки:**
   ```bash
   python manage.py runserver
   ```

6. **Откройте в браузере:**
   ```
   http://127.0.0.1:8000/
   ```

## Структура проекта

```
ovpn_control/
├── manage.py                    # Django management script
├── requirements.txt             # Python dependencies
├── README.md                    # Основная документация
├── STRUCTURE.md                 # Детальная структура проекта
├── REFACTORING_COMPLETE.md      # Информация о рефакторинге
├── static/                      # Статические файлы
│   ├── css/
│   │   └── main.css            # Основные стили
│   └── js/
│       ├── utils.js            # Утилиты
│       ├── ssh-terminal.js     # SSH терминал
│       ├── server-actions.js   # Управление серверами
│       └── client-management.js # Управление клиентами
├── ovpn_project/               # Настройки проекта
│   ├── settings.py             # Конфигурация Django
│   ├── urls.py                 # Основные маршруты
│   ├── asgi.py                 # ASGI конфигурация
│   └── wsgi.py                 # WSGI конфигурация
└── ovpn_app/                   # Основное приложение
    ├── models.py               # Модели данных
    ├── views.py                # View функции
    ├── api_views.py            # REST API views
    ├── forms.py                # Формы Django
    ├── urls.py                 # URL маршруты
    ├── api_urls.py             # API маршруты
    ├── services.py             # Бизнес-логика
    ├── ssh_service.py          # SSH сервис
    ├── openvpn_service.py      # OpenVPN сервис
    └── templates/              # HTML шаблоны
        ├── ovpn_app/
        │   ├── base.html       # Базовый шаблон
        │   ├── dashboard.html
        │   ├── server_detail.html
        │   └── clients/
        │       └── list.html
        └── registration/
            └── login.html
```

Подробная документация по структуре: [STRUCTURE.md](STRUCTURE.md)

## Использование

### Быстрый старт

1. **Добавление сервера**: Перейдите в раздел "Серверы" и добавьте новый сервер с SSH данными
2. **Генерация SSH ключа**: На странице сервера нажмите "Сгенерировать ключ" (автоматическая настройка)
3. **Установка OpenVPN**: Выберите сервер и нажмите "Установить OpenVPN"
4. **Настройка сервера**: Нажмите "Настроить сервер" для применения конфигурации
5. **Создание клиентов**: В разделе "Клиенты" создайте новые сертификаты
6. **Мониторинг**: Просматривайте активные подключения в реальном времени

### SSH ключи

Система поддерживает автоматическую генерацию SSH ключей:

- **ED25519** (рекомендуется) - современный, быстрый, безопасный
- **RSA 4096** - для совместимости со старыми системами

Подробнее: [SSH_KEY_SETUP.md](SSH_KEY_SETUP.md)

### Мониторинг VPN

```bash
# Однократный запуск
python manage.py monitor_vpn --once

# Постоянный мониторинг (каждые 30 сек)
python manage.py monitor_vpn --daemon --interval 30
```

Подробнее: [VPN_MONITORING.md](VPN_MONITORING.md)

## Технологии

- **Backend:** Django 5.2.5, Python 3.12
- **Frontend:** Bootstrap 5, Font Awesome 6
- **JavaScript:** Модульная архитектура (ES6+)
- **SSH:** asyncssh для удаленного управления
- **OpenVPN:** 2.6.14 с современной криптографией
- **Database:** SQLite (dev) / PostgreSQL (prod)
- **Code Quality:** Black, isort, Flake8, MyPy, pytest
- **Architecture:** SOLID principles, Clean Architecture

## Архитектура кода

Проект следует принципам **SOLID**, **Clean Code** и **Clean Architecture**.
Подробности в [ARCHITECTURE.md](ARCHITECTURE.md).

### Чистая структура ✅
- **HTML** - только разметка и данные
- **CSS** - только стили (main.css)
- **JavaScript** - только логика (модульный подход)
- **Python** - строгое разделение слоев (presentation, business, data, infrastructure)

### Качество кода
```bash
# Форматирование и проверка
make format    # Black + isort
make lint      # Flake8 + MyPy
make test      # Pytest
make quality   # Все проверки

# Или через скрипт
./check_code_quality.sh
```

### JavaScript модули:
- `utils.js` - общие утилиты и API функции
- `ssh-terminal.js` - интерактивный SSH терминал
- `server-actions.js` - установка, настройка, управление серверами
- `client-management.js` - создание, скачивание, отзыв сертификатов

### API endpoints:
- `POST /api/servers/{id}/install-openvpn/` - установка OpenVPN
- `POST /api/servers/{id}/configure-openvpn/` - настройка сервера
- `POST /api/servers/{id}/start-openvpn/` - запуск сервера
- `POST /api/servers/{id}/generate-ssh-key/` - генерация и установка SSH ключа
- `POST /api/servers/{id}/create-client/` - создание клиента
- `GET /api/servers/{id}/download-client/{name}/` - скачивание .ovpn
- `POST /api/servers/{id}/update-connections/` - обновление подключений
- `POST /api/connections/update-all/` - обновление всех подключений

## Документация

- **[README.md](README.md)** (этот файл) - быстрый старт и обзор
- **[STRUCTURE.md](STRUCTURE.md)** - детальная структура проекта
- **[REFACTORING_COMPLETE.md](REFACTORING_COMPLETE.md)** - информация о рефакторинге кода
- **[SSH_KEY_SETUP.md](SSH_KEY_SETUP.md)** - настройка автоматической генерации SSH ключей
- **[VPN_MONITORING.md](VPN_MONITORING.md)** - система мониторинга VPN подключений
- **[QUICKSTART_MONITORING.md](QUICKSTART_MONITORING.md)** - быстрый старт мониторинга

## Production deployment

### Сбор статики:
```bash
python manage.py collectstatic
```

### Настройка nginx:
```nginx
location /static/ {
    alias /path/to/staticfiles/;
    expires 30d;
}

location /media/ {
    alias /path/to/media/;
}
```

### Настройка sudo без пароля (важно!)

Для автоматического выполнения команд через SSH необходимо настроить sudo без запроса пароля:

```bash
# На удаленном сервере выполните:
sudo visudo

# Добавьте строку (замените username на ваше имя пользователя):
username ALL=(ALL) NOPASSWD: ALL

# Или для конкретных команд OpenVPN:
username ALL=(ALL) NOPASSWD: /usr/sbin/openvpn, /bin/systemctl, /usr/bin/cat /var/log/openvpn/*
```

⚠️ **Важно:** Без этой настройки мониторинг и управление серверами не будут работать!

### UFW Firewall:
Для работы OpenVPN с UFW необходимо настроить NAT:
```bash
# Разрешить порт OpenVPN
sudo ufw allow 443/udp

# Настроить MASQUERADE в /etc/ufw/before.rules
# Включить forwarding в /etc/default/ufw
```

Подробности в документации проекта.
